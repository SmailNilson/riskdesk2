# Frontend runtime endpoints
# Copy to frontend/.env.local and set the backend instance for your worktree.
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_WS_URL=

# Next.js local proxy target for relative /api calls
RISKDESK_API_PROXY_TARGET=http://localhost:8080
